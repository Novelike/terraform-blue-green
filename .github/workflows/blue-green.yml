# .github/workflows/blue-green.yml
name: Blue-Green Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy & Switch
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash

    env:
      # Terraform 인증
      TF_VAR_username:    ${{ secrets.TF_VAR_USERNAME }}
      TF_VAR_password:    ${{ secrets.TF_VAR_PASSWORD }}
      TF_VAR_tenant_name: ${{ secrets.TF_VAR_TENANT_NAME }}
      TF_VAR_region:      ${{ secrets.TF_VAR_REGION }}
      # SSH & Discord
      SSH_KEY:            ${{ secrets.SSH_KEY }}
      DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.7

      - name: Terraform Init & Refresh
        working-directory: terraform
        run: |
          terraform init
          terraform refresh || true

      - name: Determine Active & Next Env
        working-directory: terraform
        run: |
          set +e

          # 1) lb_vip 읽기 (stderr은 버리고)
          LB_VIP=$(terraform output -raw lb_vip 2>/dev/null)
          RET=$?
          if [[ $RET -ne 0 ]] || [[ -z "$LB_VIP" ]]; then
            echo "::warning::Could not read lb_vip; defaulting to blue"
            echo "NEXT_ENV=blue" >> $GITHUB_ENV
            exit 0
          fi

          # 2) LB_VIP 환경변수에 기록
          echo "LB_VIP=$LB_VIP"  >> $GITHUB_ENV

          # 3) 헬스체크
          curl -fs http://$LB_VIP/hello
          if [[ $? -eq 0 ]]; then
            echo "NEXT_ENV=green" >> $GITHUB_ENV
          else
            echo "NEXT_ENV=blue"  >> $GITHUB_ENV
          fi

          set -e

      - name: Get Instance IPs
        working-directory: terraform
        run: |
          echo "BLUE_IP=$(terraform output -raw blue_ip)"   >> $GITHUB_ENV
          echo "GREEN_IP=$(terraform output -raw green_ip)" >> $GITHUB_ENV

      - name: Deploy to Blue
        if: env.NEXT_ENV == 'blue'
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ env.BLUE_IP }}
          username: ubuntu
          key: ${{ env.SSH_KEY }}
          script: |
            cd /opt/app
            git pull
            . venv/bin/activate
            pip install -r requirements.txt
            echo "$(date)" > /opt/deploy_timestamp.txt
            pkill -f "uvicorn app.main"
            nohup uvicorn app.main:app --host 0.0.0.0 --port 8000 &

      - name: Deploy to Green
        if: env.NEXT_ENV == 'green'
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ env.GREEN_IP }}
          username: ubuntu
          key: ${{ env.SSH_KEY }}
          script: |
            cd /opt/app
            git pull
            . venv/bin/activate
            pip install -r requirements.txt
            echo "$(date)" > /opt/deploy_timestamp.txt
            pkill -f "uvicorn app.main"
            nohup uvicorn app.main:app --host 0.0.0.0 --port 8000 &

      - name: Health Check
        run: |
          for i in {1..5}; do
            if curl -fs http://${{ env.LB_VIP }}/hello; then
              exit 0
            fi
            sleep 5
          done
          echo "::error::Health check failed" && exit 1

      - name: Switch Traffic
        if: success()
        working-directory: terraform
        run: terraform apply -auto-approve -var="active_env=${{ env.NEXT_ENV }}"

      - name: Notify Discord
        run: |
          if [[ "${{ job.status }}" == "success" ]]; then
            MSG="✅ Deployed to **${{ env.NEXT_ENV }}** succeeded!\nLB VIP: ${{ env.LB_VIP }}"
          else
            MSG="🚨 Deployed to **${{ env.NEXT_ENV }}** failed! Check logs."
          fi
          curl -H "Content-Type: application/json" \
               -d "{\"content\":\"$MSG\"}" \
               ${{ env.DISCORD_WEBHOOK_URL }}

